#include <stdio.h>

int main(){
    int a = 13;
    int b = 4;
    int c = a + b; 
    printf("the value of a is %d and the value of b is %d and the sum of a and b is %d\n", a, b,c );
    printf("the reminder of %d / %d is : %d", a, b, a % b);
    
    return 0;
}
